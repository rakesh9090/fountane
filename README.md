# fountane

OTP is simulated and printed in Metro logs.

Google OAuth is mocked.

OTP auto-fill from SMS is not implemented because Expo managed workflow does not expose Android SMS Retriever APIs directly.

Production implementation would use Android SMS Retriever API and iOS one-time-code autofill support.
